# BLUEPRINT DEFINITIVO — Capítulo 2: Objetivos, Alcance y Antecedentes

> Instrucciones generales de redacción para todo el capítulo:
> - Registro: tercera persona impersonal, tono de memoria técnica de máster.
> - NINGUNA figura va en este capítulo. Las dos figuras conceptuales que antes
>   vivían aquí (Barren Plateaus y confinamiento Dicke/2^N) se trasladan
>   íntegramente al Capítulo 4 (Marco Teórico), porque explicar su mecanismo
>   sin las matemáticas de Ising/XY-Mixer quedaría vacío de contenido y
>   generaría la misma duplicación que criticó el tribunal.
> - Este capítulo NO debe contener ninguna derivación matemática (ni QUBO,
>   ni Ising, ni operadores). Si en algún borrador aparece una ecuación,
>   trasladarla al Capítulo 4.
> - Extensión orientativa total: 4-5 páginas (es un capítulo de posicionamiento,
>   no de desarrollo).

---

## 2.1. Objetivos del Proyecto
`\label{sec:objetivos_proyecto}`

### 2.1.1. Objetivo General

**Contenido:** párrafo único, ya aprobado, insertar literalmente:

```latex
\subsection{Objetivo General}
\label{subsec:objetivo_general}
El presente Trabajo de Fin de Máster tiene como finalidad determinar en qué
medida un algoritmo cuántico variacional con preservación de simetría
estructural (XY-QAOA regularizado) permite resolver el problema de selección
de carteras financieras bajo una restricción de cardinalidad exacta $K$ de
forma más robusta —en términos de factibilidad de las soluciones y calidad
financiera de las mismas— que el paradigma estándar de penalizaciones
cuadráticas (QAOA), delimitando honestamente el rango de escalabilidad en el
que dicha ventaja resulta observable bajo simulación clásica de vectores de
estado, y contrastando sus resultados frente a solucionadores de referencia
de uso industrial (Gurobi Optimizer) y metaheurísticas clásicas (Simulated
Annealing).
```

**Imprescindible:** que quede explícito el verbo "determinar en qué medida" (no "desarrollar" ni "implementar") — es la palabra que responde a la crítica del tribunal sobre "finalidad, no tarea".

---

### 2.1.2. Objetivos Específicos

**Contenido:** los 4 objetivos ya aprobados (OE1–OE4), insertar literalmente como itemize:

```latex
\subsection{Objetivos Específicos}
\label{subsec:objetivos_especificos}
\begin{itemize}

    \item \textbf{OE1. Verificar la equivalencia matemática del mapeo
    Markowitz $\rightarrow$ QUBO $\rightarrow$ Ising.}
    Comprobar que la energía del Hamiltoniano de Ising construido reproduce
    exactamente la función objetivo financiera original (media-varianza con
    penalización de cardinalidad) para el conjunto completo de
    configuraciones binarias en instancias de tamaño reducido ($N \le 14$),
    empleando un solucionador exacto por fuerza bruta como referencia de
    verificación. \textit{Criterio de éxito:} discrepancia numérica nula
    (dentro de la tolerancia de precisión de punto flotante) entre la energía
    de Ising y el valor QUBO en el 100\% de las configuraciones evaluadas.

    \item \textbf{OE2. Cuantificar la mejora en la factibilidad estructural
    de las soluciones cuánticas.}
    Determinar si la sustitución del mezclador transversal ($X$-Mixer) y la
    penalización cuadrática por un operador de intercambio $XY$ inicializado
    en un estado de Dicke incrementa de forma sustancial la proporción de
    muestras que satisfacen la restricción $\sum_i x_i = K$, frente al QAOA
    estándar, para un conjunto de instancias con $N \in [6, 20]$ y
    profundidades $p$ acotadas. \textit{Criterio de éxito:} diferencia
    estadísticamente relevante y creciente con $N$ entre la tasa de
    factibilidad de ambos enfoques, verificable mediante el muestreo directo
    del vector de estado simulado.

    \item \textbf{OE3. Evaluar el efecto estabilizador de la inicialización
    TQA y la regularización Ridge $L_2$ sobre la convergencia clásica.}
    Medir si la combinación de una inicialización basada en \textit{Trotterized
    Quantum Annealing} y una penalización continua $L_2$ sobre la
    trayectoria de los parámetros $(\gamma, \beta)$ reduce la variabilidad
    del resultado final del optimizador COBYLA y/o el número de evaluaciones
    de la función de coste necesarias para alcanzar un óptimo local estable,
    en comparación con una inicialización aleatoria sin regularizar, sobre
    una instancia de referencia fija. \textit{Criterio de éxito:} reducción
    observable de la desviación estándar del \textit{Optimization GAP} entre
    semillas aleatorias al introducir la regularización.

    \item \textbf{OE4. Contrastar el rendimiento del XY-QAOA regularizado
    frente a solucionadores de referencia clásicos.}
    Comparar el \textit{Optimization GAP} respecto al óptimo exacto
    (Gurobi), el Ratio de Sharpe fuera de muestra y el coste temporal de
    ejecución del XY-QAOA regularizado frente a Gurobi Optimizer y Simulated
    Annealing, utilizando series históricas reales de mercado, para
    determinar el rango de tamaños de universo $N$ en el que la propuesta
    cuántica resulta competitiva bajo simulación clásica exacta.
    \textit{Criterio de éxito:} identificación explícita del umbral de $N$ a
    partir del cual el coste de simulación deja de ser práctico, y
    caracterización cuantitativa de la relación GAP–robustez financiera en
    el rango accesible.

\end{itemize}
```

**Imprescindible:** cada OE debe leerse de forma autónoma como pregunta de investigación, no como checklist. No añadir un OE5 nuevo aunque se te ocurra algo — con estos 4 basta y están diseñados para cubrir mapeo, factibilidad, optimización clásica y benchmarking, que son las 4 patas del proyecto.

---

### 2.1.3. Hipótesis y Criterios de Éxito

**Contenido:** un párrafo puente + una tabla que sintetiza los 4 OE en formato entregable/métrica, **con lenguaje idéntico** al que reutilizarás en el Capítulo 8 para el veredicto de cumplimiento.

**Párrafo puente obligatorio (antes de la tabla):**
> Debe decir explícitamente que estos criterios serán contrastados empíricamente en el Capítulo 6 (Evaluación y Análisis de Resultados) y que su veredicto formal de cumplimiento se recoge en el Capítulo 8 (Conclusiones). Esto es lo que crea la "relación clara entre objetivos, metodología, resultados y conclusiones" que pide el feedback.

**Tabla obligatoria — Tabla 2.1:**

```latex
\begin{table}[htbp]
    \centering
    \caption{Matriz de trazabilidad de objetivos específicos e hipótesis de éxito.}
    \label{tab:hipotesis_exito}
    \begin{tabular}{cp{5.5cm}p{6cm}}
        \toprule
        \textbf{Objetivo} & \textbf{Entregable / Indicador} & \textbf{Métrica de Éxito} \\
        \midrule
        \textbf{OE1} & Verificación de equivalencia QUBO-Ising & [PENDIENTE: cerrar tras Cap.5 —
            p.ej. "coincidencia exacta en N configuraciones evaluadas por fuerza bruta"] \\
        \textbf{OE2} & Tasa de factibilidad comparada QAOA vs. XY-QAOA & [PENDIENTE: cerrar tras Cap.6
            con el valor real observado, p.ej. "100\% vs. X\% decreciente con N"] \\
        \textbf{OE3} & Dispersión del GAP entre semillas, con/sin regularización & [PENDIENTE: cerrar
            tras Cap.6 con la reducción de desviación estándar realmente medida] \\
        \textbf{OE4} & GAP, Sharpe OOS y tiempo vs. Gurobi/SA & [PENDIENTE: cerrar tras Cap.6,
            indicando el rango de N donde la propuesta es competitiva] \\
        \bottomrule
    \end{tabular}
\end{table}
```

**Nota importante:** los `[PENDIENTE]` son intencionados. NO los rellenes con las cifras que hay ahora en `results.csv` (son sintéticas, no proceden de una ejecución real — ver conversación previa). Se cerrarán con datos reales cuando trabajemos el Capítulo 5/6. Por ahora, deja el marcador para no tener que reescribir esta tabla dos veces.

---

## 2.2. Alcance y Delimitación del Estudio
`\label{sec:alcance_delimitacion}`

**Contenido:** reutilizar casi literalmente el texto ya existente en `2.ObjetivosAlcanceYAntecedentes.tex`, secciones "Fronteras del Proyecto e Inclusiones Técnicas" y "Exclusiones Metodológicas y Limitaciones NISQ". No requiere reescritura sustancial, solo dos ajustes:

### 2.2.1. Inclusiones Técnicas
**Imprescindible mantener los 3 puntos:**
1. Modelado financiero con datos históricos reales de renta variable.
2. Simulación cuántica exacta de vectores de estado hasta $N=20$ (aclarar aquí, en una frase, que esto es un límite de memoria RAM impuesto por el crecimiento $2^N$, no una elección arbitraria — esto conecta con la honestidad científica que pide el tribunal).
3. Soporte para restricción de cardinalidad y restricciones presupuestarias vía penalización diagonal.

**Ajuste obligatorio respecto a la versión anterior:** añadir una frase que dirija al lector al Capítulo 5 para el detalle exacto de qué activos, qué ventanas temporales y qué fuente de datos se usaron ("el detalle de la fuente de datos, el preprocesado y los hiperparámetros empleados se desarrolla en el Capítulo 5"). Esto evita que el lector espere aquí el detalle que realmente pide el feedback y que vive en otro capítulo.

### 2.2.2. Exclusiones Metodológicas y Limitaciones NISQ
**Imprescindible mantener los 4 puntos:** mitigación de ruido hardware real, optimización multiperiodo, tiempos de cola/latencia de red, HFT. Reutilizar el texto existente casi sin cambios.

**Sin tablas ni figuras en esta sección.**

---

## 2.3. Antecedentes y Estado del Arte
`\label{sec:antecedentes_estado_arte}`

> Regla de oro de esta sección: **cada subapartado cita literatura y describe limitaciones, pero NUNCA desarrolla la solución técnica propia.** La solución (XY-Mixer, Dicke, TQA, Ridge) se reserva íntegra para el Capítulo 4. Si en algún momento sientes que "para explicar la limitación necesito la fórmula", es señal de que esa frase pertenece al Capítulo 4, no a este.

### 2.3.1. De Markowitz al Problema Combinatorio

**Contenido:**
- Mencionar brevemente el modelo media-varianza de Markowitz (una frase, sin ecuación completa — como mucho la ecuación conceptual $\min w^T\Sigma w - \mu^Tw$ ya usada en el Cap. 1, sin repetirla entera).
- Explicar en prosa (sin derivar) que la introducción de variables binarias para forzar cardinalidad exacta rompe la naturaleza continua del problema y lo traslada a la clase NP-hard.
- Mencionar el crecimiento combinatorio gobernado por $\binom{N}{K}$, **sin repetir la tabla numérica** — en su lugar, referenciar con `\ref{tab:dimension_espacio}` la tabla ya presentada en el Capítulo 1 (evita duplicación exacta de contenido, otro punto que valorará positivamente el tribunal).

**Imprescindible:** cita a `Markowitz1952`. Frase de cierre que conecte con 2.3.2 ("ante esta explosión combinatoria, la industria recurre a dos familias de solucionadores clásicos...").

**Sin tabla ni figura propia** (se referencia la del Cap. 1).

### 2.3.2. Solucionadores Clásicos de Referencia

**Contenido:**
- **Gurobi Optimizer:** describir en 3-4 líneas que resuelve MIQP mediante Branch-and-Bound y planos de corte, que garantiza optimalidad global, pero que su tiempo de ejecución en el peor caso escala exponencialmente. Cita `Gurobi2025`.
- **Simulated Annealing:** describir en 3-4 líneas que es una metaheurística estocástica inspirada en el recocido de metales, rápida y escalable, pero sin certificado de optimalidad global ni garantía de reproducibilidad exacta entre semillas.

**Imprescindible:** dejar explícito que estos dos son los "puntos de referencia" (baselines) contra los que se compara todo en el Capítulo 6 — esto ya prepara al lector para el benchmarking posterior.

**Sin figuras.** Opcionalmente puedes reutilizar aquí, en prosa, una frase que anticipe la Tabla 2.2 de cierre del capítulo (sin repetirla).

### 2.3.3. Computación Cuántica Variacional: de Quantum Annealing a QAOA

**Contenido:**
- Evolución histórica breve: sistemas analógicos de Quantum Annealing (recocido cuántico nativo en redes de espín) → limitaciones de conectividad física → migración hacia circuitos de compuertas parametrizadas.
- Presentar QAOA como el modelo de referencia en este paradigma digital-híbrido. Explicar en una frase su lógica general (alternancia de Hamiltoniano de coste y mezclador), **sin fórmulas de los operadores** (eso va al Cap. 4).
- Mencionar que QAOA estándar requiere mapear las restricciones mediante penalizaciones cuadráticas integradas en la función de coste.

**Imprescindible:** citas a `Farhi2014` y `Preskill2018`.

**Sin figuras ni ecuaciones de operadores.**

### 2.3.4. Limitaciones Documentadas del QAOA Estándar

**Contenido:**
- Explicar, en prosa y sin derivación, que la introducción de coeficientes de penalización de gran magnitud deforma el paisaje energético de la función de coste.
- Mencionar el fenómeno de *Barren Plateaus* como hallazgo documentado en la literatura (gradientes que se desvanecen con el número de cúbits), citando `McClean2018`, **sin desarrollar el mecanismo matemático ni incluir la figura conceptual** (eso se traslada íntegro al Capítulo 4, donde se conecta directamente con la solución TQA + Ridge).
- Cerrar con una frase que indique que estas dos limitaciones (penalización + barren plateaus) son precisamente la motivación técnica que este TFM aborda mediante preservación de simetría estructural.

**Imprescindible:** que quede clarísimo que aquí solo se *señala el problema documentado por terceros*, no se explica la solución propia. Esta distinción es la que evita la duplicación con el Capítulo 4.

**Sin figura en este punto** (la figura de Barren Plateaus se mueve al Capítulo 4).

---

## 2.4. Síntesis Comparativa y Posicionamiento del Trabajo
`\label{sec:sintesis_posicionamiento}`

**Contenido:**
- Un párrafo de cierre que actúe como **bisagra explícita hacia el Capítulo 4**: debe decir con todas las letras que, frente a las limitaciones señaladas en 2.3, el Capítulo 4 desarrollará formalmente la arquitectura técnica propuesta (mapeo Ising, XY-Mixer con preservación de simetría, estados de Dicke, TQA y regularización Ridge $L_2$) que este trabajo implementa y evalúa.
- Inmediatamente después (o antes, a elección de redacción), incluir la tabla comparativa taxonómica que cierra el capítulo con una vista de pájaro de todo el estado del arte, incluyendo ya la fila de "XY-QAOA (Propuesto)" pero **describiéndola solo a nivel conceptual** (una frase por celda, sin fórmulas).

**Tabla obligatoria — Tabla 2.2** (reutilizar la que ya existe en el `.tex` actual, con ligero ajuste de redacción en la última fila para que no adelante matemáticas):

```latex
\begin{table}[htbp]
    \centering
    \caption{Comparativa taxonómica del estado del arte en algoritmos de optimización combinatoria clásica y cuántica.}
    \label{tab:estado_arte}
    \begin{tabular}{p{2.8cm}p{2.5cm}p{3cm}p{2.2cm}p{2.3cm}}
        \toprule
        \textbf{Algoritmo / Enfoque} & \textbf{Paradigma de Computación} & \textbf{Manejo de Restricciones ($K$)} & \textbf{Eficiencia en Cúbits / Recursos} & \textbf{Escalabilidad en la Era NISQ} \\
        \midrule
        \textbf{Gurobi Optimizer} \cite{Gurobi2025} & Clásico Determinista Exacto & Planos de corte y bifurcación ramificada. & Memoria RAM y CPU escalar exponencial. & Nula para instancias densas ($N \gg 100$). \\
        \addlinespace
        \textbf{Simulated Annealing} & Clásico Estocástico Heurístico & Penalización o rechazo analítico de vecindarios. & Cómputo CPU secuencial ligero. & Alta rapidez temporal, baja garantía de optimalidad global. \\
        \addlinespace
        \textbf{Standard QAOA} \cite{Farhi2014} & Cuántico Basado en Compuertas & Penalización cuadrática en la función de coste. & Requiere $N$ cúbits sin registros auxiliares. & Baja (alto ruido por penalizaciones duras e inestabilidad). \\
        \addlinespace
        \textbf{Quantum Annealing} & Cuántico Adiabático Nativo & Mapeo de penalizaciones QUBO en grafos físicos. & Alta dependencia del grafo de acoplamiento. & Media (afectado por las limitaciones de conectividad de la arquitectura). \\
        \addlinespace
        \textbf{XY-QAOA (Propuesto)} & Cuántico Híbrido Variacional & Confinamiento físico en subespacio combinatorio reducido (desarrollado en el Cap. 4). & Optimizado a $N$ cúbits lineales libres de ancillas. & Alta (menor profundidad de circuito y resistencia al ruido \cite{Barkoutsos2020}). \\
        \bottomrule
    \end{tabular}
\end{table}
```

**Imprescindible:** la última celda de la fila XY-QAOA debe remitir explícitamente al Capítulo 4 ("desarrollado en el Cap. 4"), no explicar el mecanismo aquí. Cita a `Barkoutsos2020` en esta fila.

**Cierre del capítulo:** última frase debe ser literalmente de transición, del estilo: *"El Capítulo 4 retoma este posicionamiento para formalizar matemáticamente la arquitectura XY-QAOA regularizada, cuya implementación y validación experimental se detallan en los Capítulos 5 y 6 respectivamente."* — esto es lo que teje la coherencia entre capítulos que pide el tribunal.

---

## Checklist final antes de dar por cerrado el capítulo

- [ ] Objetivo general formulado como finalidad, no como tarea.
- [ ] Los 4 OE tienen criterio de éxito explícito y verificable.
- [ ] Tabla 2.1 con marcadores `[PENDIENTE]` claramente señalados para cerrar tras Cap. 5/6.
- [ ] Cero ecuaciones de QUBO/Ising/XY-Mixer/TQA/Ridge en todo el capítulo.
- [ ] Cero figuras en todo el capítulo (ambas se trasladan al Cap. 4).
- [ ] Ninguna tabla numérica duplicada respecto a la Tabla 1.1 del Capítulo 1 (se referencia, no se repite).
- [ ] Última frase del capítulo remite explícitamente al Capítulo 4.
- [ ] Citas usadas: `Markowitz1952`, `Gurobi2025`, `Farhi2014`, `Preskill2018`, `McClean2018`, `Barkoutsos2020` (todas ya en `bibliografia.bib`, no falta ninguna).